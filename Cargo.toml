[package]
edition = "2024"
name = "audio-blocks"
version = "0.1.0"

[dependencies]
rtsan-standalone = "0.1.1"

[dev-dependencies]
criterion = "0.5.1"

[features]
default = ["std"]

alloc = []
rtsan = ["rtsan-standalone/enable"]
std = []

[[bench]]
harness = false
name = "blocks"
